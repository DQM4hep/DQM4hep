/// \file DQMArchiver.h
/*
 *
 * DQMArchiver.h header template automatically generated by a class generator
 * Creation date : mar. oct. 7 2014
 *
 * This file is part of DQM4HEP libraries.
 * 
 * DQM4HEP is free software: you can redistribute it and/or modify
 * it under the terms of the GNU General Public License as published by
 * the Free Software Foundation, either version 3 of the License, or
 * (at your option) any later version.
 * based upon these libraries are permitted. Any copy of these libraries
 * must include this copyright notice.
 * 
 * DQM4HEP is distributed in the hope that it will be useful,
 * but WITHOUT ANY WARRANTY; without even the implied warranty of
 * MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the
 * GNU General Public License for more details.
 * 
 * You should have received a copy of the GNU General Public License
 * along with DQM4HEP.  If not, see <http://www.gnu.org/licenses/>.
 * 
 * @author Remi Ete
 * @copyright CNRS , IPNL
 */


#ifndef DQM4HEP_ARCHIVER_H
#define DQM4HEP_ARCHIVER_H

// -- dqm4hep headers
#include "dqm4hep/DQM4HEP.h"

class TFile;
class TTree;
class TBranch;
class TDirectory;

namespace dqm4hep {

  namespace core {

    class Module;
    class Directory;
    class MonitorElementManager;
    class Storage;

    /** Archiver class
     */
    class Archiver
    {
      friend class MonitorElementManager;
    public:
      /** Constructor
       */
      Archiver();

      /** Constructor with file name and opening mode
       */
      Archiver(const std::string &archiveFileName, const std::string &openingMode = "RECREATE");

      /** Destructor
       */
      virtual ~Archiver();

      /** Open a new archive.
       *  Close the current file if opened. Supported opening mode are the TFile opening mode option (see TFile)
       */
      StatusCode open(const std::string &archiveFileName, const std::string &openingMode = "RECREATE");

      /** Close the current archive
       */
      StatusCode close();

      /** Archive the monitor element list of the given module
       *  Monitor elements that are set to "non-publish" can
       *  be written passing the flag to true (default)
       */
      StatusCode archive(Module *pModule, bool archiveAll = true);

      /** Archive the current state of the storage in the root TDirectory
       */
      StatusCode archive(Storage *pStorage, TDirectory *pDirectory);

      /** Get the file name
       */
      const std::string &getFileName() const;

      /** Whether the archive is opened
       */
      bool isOpened() const;

      /** Get the opening mode of this archive
       */
      const std::string &getOpeningMode() const;

      /** Fill recursively the TDirectory with the Directory
       */
      static StatusCode recursiveFill(Directory *pDirectory, TDirectory *pROOTDir);

      /** Create TDirectories as labeled in the Directory
       */
      static StatusCode createTDirectories(Directory *pDirectory, TDirectory *pROOTDir);

      /** Write the monitor elements contained in the Directory in the TDirectory
       */
      static StatusCode writeMonitorElements(Directory *pDirectory, TDirectory *pROOTDir);

    private:
      // members
      std::string           m_fileName;
      std::string           m_openingMode;
      bool                  m_isOpened;
      bool                  m_firstArchive;

      TFile                *m_pArchiveFile;
    };

  }

} 

#endif  //  DQM4HEP_ARCHIVER_H
